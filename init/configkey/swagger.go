package configkey

const SwaggerTitle = "swagger.title"

const SwaggerDescription = "swagger.description"

const SwaggerVersion = "swagger.version"

const SwaggerHost = "swagger.host"

const SwaggerBasePath = "swagger.basePath"
