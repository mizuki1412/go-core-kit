package configkey

// LogPath 日志目录: 不设置则不写入文件
const LogPath = "logger.path"

// LogName 文件名，无后缀
const LogName = "logger.name"

// LogMaxRemain 最大保留天数
const LogMaxRemain = "logger.max-remain"

// LogMaxBackups 最大保留个数
const LogMaxBackups = "logger.max-backups"

// LogMaxSize 单文件最大size： megabytes
const LogMaxSize = "logger.max-size"

const LogLevel = "logger.level"

const LogType = "logger.type"
